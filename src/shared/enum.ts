export enum HoleListMode {
  latest = 'latest',
  hot = 'hot',
}

export enum HoleDetailCommentMode {
  all = 'all',
  author = 'author',
}

export enum HoleDetailCommentOrderMode {
  favorite = 'favorite',
  time = 'time',
}
